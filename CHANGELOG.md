# 更新记录

本项目遵循语义化版本号，`MAJOR.MINOR.PATCH` 分别表示不兼容变更、新功能与修复整理。

## 2.5.0 - 2026-08-02

### 国内公开来源检索

- 公开来源核验新增博查 Web Search 适配器，并设为新配置的国内默认检索服务；使用其结构化网页结果发现潜在证据来源，再由本地规则完成主张覆盖与冲突比对。
- 设置页新增搜索服务选择器：可选“博查 Web Search（国内默认）”或“Brave Search（国际来源）”，连接测试、保存配置与实际联网核验始终使用同一提供方。
- 保留 Brave 适配器与旧本机配置兼容性；已有 `SEARCH_PROVIDER=brave` 和 Key 的用户无需迁移即可继续使用。
- 搜索结果缓存按提供方隔离，防止切换服务后复用旧来源；API Key 仍只写入本机 `.env`，不进入浏览器存储、历史、导出、日志或接口响应。
- 新增博查请求/响应映射、默认能力状态、提供方保存、Key 不回传和前端配置状态测试；未配置任何 Key 时继续完整使用离线核验。

### AI 证据解读兼容性修复

- 兼容会先输出推理内容的 `deepseek-v4-flash`：将用户主动发起的 AI 证据解读输出预算调整为 5,200 tokens、请求上限调整为 45 秒，避免模型在生成最终 JSON 前耗尽响应预算。
- 结构化 JSON、主张全覆盖、来源编号与高可信来源引用校验保持不变；超时或格式异常仍安全保留规则核验结果，不生成真实性结论。

## 2.4.1 - 2026-08-01

### 新闻链接提取兼容性

- 将单一的全页段落读取升级为多策略静态提取：依次利用 Open Graph/Twitter 标题、Schema.org JSON-LD、常见前端框架内嵌 JSON、央视网 `contentdate`、新闻正文语义容器及全页段落回退。
- 新增常见中文新闻 CMS 容器选择器，并按正文长度、段落数量与结构可信度选取最佳候选内容，过滤导航、侧栏、页脚、视频占位和常见版权/推荐文案。
- 新增 UTF-8、GB18030/GBK、Big5 及页面/响应声明编码识别，减少旧式中文新闻站点出现乱码或空正文的问题。
- 保持不执行页面 JavaScript 的安全边界；登录墙、付费墙、强反爬及完全动态装配页面仍会提示用户改用原始报道页或手动粘贴，系统不伪造不完整正文。
- 新增 JSON-LD、语义容器、前端框架数据和 GB18030 页面回归测试；实际复验央视网公开新闻链接可提取准确标题与 3,703 字正文。

## 2.4.0 - 2026-08-01

### 新闻链接导入

- 新增公开 HTTPS 新闻链接导入：提取标题与正文后仅填入可编辑工作台，用户确认后才会生成摘要或保存历史。
- 复用受限公网抓取链路，拒绝内网地址、非法协议、非 HTML、超大页面和不安全跳转；动态页面、付费墙或正文不足时提示手动粘贴。
- 历史记录、JSON 备份和文本导出新增可选来源链接与域名，不保存远程网页全文、Cookie 或响应头。
- 修复草稿来源元数据被误传给严格摘要接口的问题；手动输入、样例加载和链接导入后的摘要生成均只提交接口允许字段。
- 工作台与后端会在请求网页前识别直接图片、视频资源链接并给出明确提示，避免无效抓取；含图片或视频的新闻报道 HTML 页面仍按正文提取。
- 工作台与后端会识别 MSN 动态新闻页并给出专门说明，提示改用原始发布媒体的报道链接或手动复制文字正文；不引入浏览器爬虫来执行第三方网页脚本。
- 支持央视网等在静态 HTML 中以 `contentdate` 变量内嵌正文的新闻页；解析过程不执行页面脚本，并过滤内嵌视频占位内容。
- 将主导航中的“评测中心”调整为页脚“算法与质量说明”辅助入口，移除普通用户无需使用的本地评测运行按钮；保留评测接口与方法说明用于后续开发验证。

## 2.3.0 - 2026-08-01

### 本机历史备份与恢复

- 新增完整历史 JSON 备份与导入，保留摘要、事实、原文依据、证据约束、评分、收藏和已保存的核验摘要。
- 导入前执行严格结构校验，单次最多处理 200 条记录；导入只合并新记录，以正文和摘要识别重复内容，不覆盖或删除原有记录。
- 备份不包含 API Key、远程网页全文、SQLite 数据库或浏览器配置；历史数据始终由用户手动下载和选择导入。

## 2.2.1 - 2026-08-01

### 课程交付整理

- 调整项目文档定位为高质量课程实训交付，明确本地离线流程、真实已验证测试与未完成评测之间的边界。
- 新增 MIT License 和课程提交检查清单，便于公开仓库维护与后续报告整理。
- 删除重复的离线核验截图引用，保留唯一有效截图作为 V2.1/V2.2 的共同离线验收证据。

## 2.2.0 - 2026-07-20

### AI 辅助证据解读

- 联网核验完成后，用户可主动调用已配置的 DeepSeek/OpenAI 兼容服务，对已有公开来源短摘录进行证据解读。
- AI 建议与规则核验状态并列显示，必须引用当前来源编号，不能联网、不能覆盖规则状态、不能输出新闻真伪结论。
- 新增主张全覆盖、来源存在性、官方/主流媒体高可信引用和模型 JSON 输出校验；异常时安全保留原规则核验结果。
- 历史记录可保存并恢复 AI 建议；重新生成摘要或重新联网核验会清空旧建议，防止证据错配。

## 2.1.0 - 2026-07-20

### 新闻核验线索

- 新增本地可追溯主张与“核验线索”阅读标签；系统只展示证据状态，不输出新闻真伪裁决。
- 新增可选 Brave Search API 配置、连接测试、公开来源分级、短查询检索和确定性证据匹配；联网操作只由用户主动触发。
- 增加 HTTPS、私有地址拒绝、跳转/响应大小/超时限制及十分钟内存缓存；远程网页全文不写入历史记录。
- 历史记录新增核验结果摘要兼容存储；旧记录会自动获得基于原文依据的离线核验预览。

## 2.0.0 - 2026-07-19

### 可信新闻理解

- 增加主体、时间、地点、核心事件、关键数值、影响与后续六要素事实卡，全部附带原文句依据。
- 本地 MMR 选句增加事实覆盖奖励，摘要质量新增事实覆盖率、已发现事实数和已覆盖事实数。
- 增加固定句与排除句约束；约束同时作用于单次生成、长度对比、历史保存和恢复。

### 竞赛工程与材料

- 增加可信度实验室、私有评测集运行器、公开元数据规范与人工盲评规则。
- 增加单机离线演示入口、Ruff、ESLint、GitHub Actions、竞赛演示脚本、海报结构与答辩问题库。
- 将前端和后端版本统一为 2.0.0，历史记录兼容新增事实与证据约束字段。

## 1.4.0 - 2026-07-19

### 新增

- 新闻感知本地摘要排序：PageRank、标题相关度、导语位置与数字事实信号。
- 本地简短、标准、详细三档摘要对比，以及选中版本复用流程。
- 摘要依据完整度、重复风险与原文定位展示。

### 工程整理

- 历史保存接口采用严格的 Pydantic 结果模型与原文索引校验。
- 后端测试改为内存 SQLite，避免接触用户的本机历史数据。
- 增加 Prettier、EditorConfig、格式检查和统一测试命令。
- 移除未使用的 Vite 示例组件、素材与图标。
